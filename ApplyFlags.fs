module ApplyFlags

open xivModdingFramework.Models.DataContainers

//let applyFlags (models: TTModel list) : TTModel list =
//    for model in models do
//        printfn $"Slot: {model.}"

//    let model